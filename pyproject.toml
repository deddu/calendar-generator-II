[project]
name = "calendar"
version = "0.1.0"
description = "Generate A0 calendar PNGs (and CSV) from a TOML config."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "holidays>=0.57",
    "pillow>=10.4.0",
]
